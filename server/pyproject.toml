[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "appwrite>=15.0.0",
    "flask>=3.1.2",
    "gunicorn>=25.0.1",
    "python-dotenv>=1.2.1",
]
